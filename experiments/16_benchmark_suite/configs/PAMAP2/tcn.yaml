experiment:
  name: TCN_PAMAP2

dataset:
  name: PAMAP2
  window_size: 128
  channels: 6

model:
  name: TCN

training:
  epochs: 100
  batch_size: 64
  optimizer: Adam
  learning_rate: 0.001
  seed: 42

augmentation:
  reliability_training: false
